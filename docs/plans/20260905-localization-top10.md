# Локализация на 10 самых популярных языков App Store

## Overview

Приложение говорит на пяти языках: русский (исходный), английский, немецкий,
французский, испанский. Задача — довести его до **десяти самых популярных
языков App Store**, то есть добавить пять недостающих: японский, китайский
упрощённый, корейский, португальский (Бразилия) и итальянский.

Почему именно эти пять — это **продуктовая гипотеза**, а не измеренный факт:
источника с долями рынка под рукой нет, и приёмкой плана список быть не может.
Взята общепринятая десятка языков крупнейших рынков App Store по выручке:
английский, китайский упрощённый, японский, корейский, немецкий, французский,
испанский, португальский (Бразилия), итальянский, русский. Пять из них у нас
уже есть, значит работа — ровно в пяти оставшихся, и после неё список закрыт
целиком, а не «ещё десять сверху».

Что это даёт: Япония, Китай и Корея — три из пяти крупнейших рынков платных
приложений, а Бразилия и Италия закрывают Латинскую Америку и юг Европы.
Приложение про общие расходы не завязано на язык предметно: делить счёт в кафе
одинаково нужно везде.

## Assumptions

Решения приняты без уточняющих вопросов, чтобы не блокировать работу. Каждое
можно пересмотреть — они вынесены сюда именно поэтому.

1. **Десять — это итог, а не прибавка.** Добавляем пять языков к пяти
   существующим. Альтернатива («ещё десять сверху», всего пятнадцать) удваивает
   объём и добавляет арабский с RTL — это отдельный проект.
2. **Бот остаётся на двух языках, но пуши — нет.** `api.DefineLang` умеет
   только `ru`/`en`, тексты бота живут в `conf/lang/*.ini`, и переводить их на
   десять языков смысла нет: аудитория бота русскоязычная. Но **push-уведомления
   это не бот, а канал самого приложения**, и сейчас они собираются русскими
   литералами прямо в коде (`internal/bot/notifier.go`, 13 мест вида
   `fmt.Sprintf("%s добавил расход «%s» — ваша доля %s", …)`). Японец получал бы
   русский пуш на каждый расход. Пуши локализуются — см. Задачу 8.
3. **Скриншоты магазина сначала не локализуем.** Apple и Google разрешают
   одному набору обслуживать все локали. Семь экранов × размеры устройств × пять
   языков — это отдельный конвейер (`asc-shots-pipeline`), и запускать его до
   того, как в новых странах появятся установки, преждевременно.
4. **Бренд «Splitor» остаётся, хвост названия переводится.** Поле `name` в
   App Store уже локализовано: `Splitor: общие расходы`, `Splitor: Kosten
   teilen`, `Splitor : partage de frais`. Новые локали получают ту же схему —
   бренд плюс короткое пояснение, всё вместе в 30 символов.
5. **RTL не затрагивается.** Ни один из пяти новых языков не пишется
   справа налево, зеркалить интерфейс не нужно.

## Context (from discovery)

- **iOS**: String Catalog `ios/Splitty/Localizable.xcstrings` — **634 ключа**,
  исходный язык `ru`, из них **6 с множественными формами**; отдельно
  `InfoPlist.xcstrings` — 4 ключа (тексты системных разрешений). Языки
  перечислены в `ios/project.yml`: `CFBundleLocalizations: [ru, en, es, de, fr]`.
- **Android**: `android/app/src/main/res/values*/strings.xml` — 686 строк, из
  них **9 помечены `translatable="false"`**, то есть к переводу **677**, плюс
  **4 plurals**. Переводы в `values-ru`, `values-de`, `values-fr`, `values-es`.
  Копировать все 686 нельзя: непереводимые дадут lint `ExtraTranslation`,
  фатальный в `lintVitalRelease`.
- **Защита от пропусков уже есть на обоих клиентах**: `LocalizationCatalogTests`
  (iOS) и `LocalizationCatalogTest` + `HardcodedTextTest` (Android) роняют
  набор, если перевод отсутствует или строка зашита в код. Это главный рычаг
  плана: расширяем список языков в тестах — и они сами показывают весь фронт
  работ.
- **Форматирование**: `Money.locale` и `DateFmt.locale` берут `Locale.current` с
  подменой для тестов — суммы и даты подхватят новую локаль сами.
- **Витрина App Store**: пять локалей (`ru`, `en-US`, `de-DE`, `fr-FR`,
  `es-ES`), приложение `6787746052`. Источник правды — **репозиторий**, не ASC:
  `metadata/version/<версия>/<локаль>.json` (поля `description`, `keywords`,
  `promotionalText`, `supportUrl`, `whatsNew`) и `metadata/app-info/<локаль>.json`
  (`name`, `subtitle`), заливка — `ios/asc/push_metadata.py`, где список локалей
  зашит в строке 124.
- ⚠️ **Свободной версии для витрины сейчас нет**: 1.8 — `READY_FOR_SALE`,
  1.9 — `WAITING_FOR_REVIEW`. Новые локали попадут в **следующую версию**, и её
  сборка обязана содержать эти языки в бандле: витрина на японском для
  приложения без японского ревью не пройдёт.
- **Подписки**: `Splitor Plus` (`6804658207`, `6804658054`) локализованы в те же
  пять локалей — новые языки нужны и там, иначе в системном диалоге покупки имя
  тарифа покажется по-английски.
- **Play**: `metadata/play/{en-US,ru-RU,de-DE,fr-FR,es-ES}.json`, поля `title`,
  `shortDescription`, `fullDescription`, `whatsNew`.
- **Прецедент**: коммит `a06c91a` «сто тридцать строк показывались по-русски на
  любом языке» — этот класс ошибок в проекте уже случался, и поймал его именно
  каталожный тест.

## Development Approach

- **testing approach**: Regular — переводы добавляются, затем каталожные тесты
  подтверждают полноту. TDD здесь вырождается: тест уже написан, он просто
  краснеет на новых языках, пока переводы не залиты.
- каждая задача доводится до конца перед следующей;
- **каждая задача обязана заканчиваться зелёными тестами**;
- ключевое отличие от обычной фичи: **тесты здесь — это и есть приёмка**.
  Расширенный список языков в каталожном тесте краснеет ровно на тех ключах,
  которых не хватает, и служит рабочим списком;
- ⚠️ **не переводить машинально и не сдавать непрочитанным**: 634 + 686 строк —
  это тот объём, где пара нелепых формулировок гарантированно проскочит.
  Каждая локаль вычитывается отдельным проходом (Задача 12).

## Testing Strategy

- **unit tests**: каталожные тесты обоих клиентов расширяются списком языков в
  первой же задаче и с этого момента охраняют весь план;
- **проверка длин**: поля витрины имеют жёсткие лимиты (подзаголовок 30,
  ключевые слова 100, описание 4000) — проверяются до заливки, а не по факту
  отказа;
- **живая проверка**: симулятор с языком системы, отличным от русского, —
  единственный способ увидеть перенос строк и обрезание в CJK;
- **e2e**: UI-тесты клиентов запускаются по одному с внешней оркестрацией и
  языконезависимы, отдельных сценариев не требуют.

## Progress Tracking

- выполненное отмечать `[x]` сразу;
- новые задачи добавлять с префиксом ➕;
- блокеры — с префиксом ⚠️;
- план держать в синхроне с фактической работой.

## Solution Overview

Порядок продиктован тем, где ошибка дороже. Сначала **клиенты** (интерфейс
видят все, кто установил), потом **витрина** (её видят те, кто ещё не
установил), потом **проверка живьём**.

Внутри клиентов порядок такой: сначала расширяем списки языков и каталожные
тесты — они краснеют и дают точный перечень работы; затем заливаем переводы;
затем отдельным проходом вычитываем. Такой порядок не даёт «потерять» строку:
пока тест красный, работа не закончена.

Витрина делится на три независимые части: метаданные версии (описание,
ключевые слова), данные уровня приложения (подзаголовок) и локализация
подписок. Последнюю легко забыть, а она видна в системном диалоге покупки.

## Technical Details

**Коды локалей по системам** — они отличаются, и это регулярный источник ошибок:

| Язык | iOS / ASC | Android | Play |
|---|---|---|---|
| японский | `ja` | `values-ja` | `ja-JP` |
| китайский упрощённый | `zh-Hans` | `values-zh-rCN` | `zh-CN` |
| корейский | `ko` | `values-ko` | `ko-KR` |
| португальский (Бразилия) | `pt-BR` | `values-pt-rBR` | `pt-BR` |
| итальянский | `it` | `values-it` | `it-IT` |

**Множественные формы.** У японского, китайского и корейского **одна** форма
(`other`) — остальные заполнять не нужно и вредно. У португальского и
итальянского две (`one`, `other`). Для сравнения: у русского четыре. Ключей с
формами: 6 в iOS, 4 в Android.

**Регистр вежливости.** Витрина и интерфейс — формальная речь: японский на
です/ます, корейский на 해요체, португальский и итальянский на «você»/«Lei».
Это не стилистика, а требование к тону магазинных описаний.

**Ключевые слова не переводятся.** Их подбирают заново под то, как ищут в
конкретном магазине: японец ищет «割り勘», а не перевод слова «split».

**CJK и вёрстка.** Японский и китайский заметно короче русского, корейский
сопоставим — обрезание маловероятно, а вот переносы строк работают иначе (в
китайском нет пробелов между словами). Проверяется только глазами на
симуляторе.

## What Goes Where

- **Implementation Steps** — всё, что делается в этом репозитории: переводы,
  тесты, конфигурация, заливка метаданных через `asc`.
- **Post-Completion** — то, что требует внешних действий: живая проверка на
  устройстве, наблюдение за установками в новых странах, решение про скриншоты.

## Implementation Steps

### Task 1: Языки в конфигурации и починка каталожных тестов

Тесты — приёмка всего плана, поэтому чинятся первыми. Сейчас iOS-тест требует
форму `one` у **любого** нерусского языка (`LocalizationCatalogTests.swift:85`),
а у японского, китайского и корейского её нет по правилам CLDR — без правки
Задача 3 не сможет позеленеть в принципе.

**Files:**
- Modify: `ios/project.yml`
- Modify: `ios/SplittyTests/LocalizationCatalogTests.swift`
- Modify: `android/app/src/test/java/com/zagir/splitty/LocalizationCatalogTest.kt`

- [ ] добавить `ja, zh-Hans, ko, pt-BR, it` в `CFBundleLocalizations` в `ios/project.yml`
- [ ] заменить в iOS-тесте `language == "ru" ? [...] : ["one","other"]` на таблицу форм CLDR по языку: `ru` → one/few/many/other, `ja`/`zh-Hans`/`ko` → только other, остальные → one/other
- [ ] добавить в iOS-тест проверку, что ЛИШНИХ форм нет: сейчас её нет вовсе, и `one` у японского прошёл бы незамеченным
- [ ] в плюрал-ветке проверять `state == "translated"`: сейчас проверяется только непустота значения (`LocalizationCatalogTests.swift:89-94`), то есть непереведённая форма проходит
- [ ] поднять обрезание диагностики: iOS режет списки до 10/15, Android до 10 — исполнителю нужен полный перечень, а не первые десять
- [ ] усилить андроидный тест: он сравнивает только МНОЖЕСТВА имён по regex, поэтому файл из 677 английских копий с потерянными подстановками пройдёт как валидный. Проверять непустоту, отличие от базовой строки и совпадение набора подстановок (`%s`, `%1$s`)
- [ ] расширить список локалей в андроидном `LocalizationCatalogTest`, переименовать `testAppBundleCarriesAllFiveLocalizations` — «пяти» в имени станет враньём
- [ ] выполнить `xcodegen generate` (проект не в git, правка `project.yml` без этого ни на что не влияет)
- [ ] убедиться, что оба теста красные — это ОЖИДАЕМОЕ состояние на стыке задач 1–3, единственное исключение из правила «задача заканчивается зелёными тестами»; зелёными они становятся в конце Задачи 3

### Task 2: Перевести String Catalog iOS

**Files:**
- Modify: `ios/Splitty/Localizable.xcstrings`

- [ ] перевести 634 ключа на `ja`, `zh-Hans`, `ko`, `pt-BR`, `it`, опираясь на русский оригинал и английский перевод
- [ ] выставить `state: translated` каждому переводу
- [ ] проверить строки с несколькими подстановками: в японском и корейском порядок аргументов другой, нужны позиционные `%1$@`, `%2$@`
- [ ] прогнать `LocalizationCatalogTests`

### Task 3: Множественные формы iOS по правилам CLDR

**Files:**
- Modify: `ios/Splitty/Localizable.xcstrings`

- [ ] у 6 ключей с `variations.plural` заполнить `ja`, `zh-Hans`, `ko` ТОЛЬКО формой `other`
- [ ] `pt-BR` и `it` — формы `one` и `other`
- [ ] помнить, что в `pt-BR` ноль попадает в форму `one` («0 participante»), а не в `other`
- [ ] прогнать тесты — они теперь знают таблицу CLDR из Задачи 1

### Task 4: Тексты системных разрешений iOS

**Files:**
- Modify: `ios/Splitty/InfoPlist.xcstrings`

- [ ] перевести 4 ключа (камера, микрофон, фотографии, распознавание речи) на пять языков
- [ ] держать формулировку по правилам Apple: одно предложение, зачем именно приложению доступ
- [ ] завести проверку `InfoPlist.xcstrings`: каталожный тест его не читает вовсе (`LocalizationCatalogTests.swift:14-17`), пропущенный перевод разрешения ничем не ловится
- [ ] собрать приложение и проверить диалог разрешения в симуляторе на японском
- [ ] прогнать тесты

### Task 5: Перевести строки Android

**Files:**
- Create: `android/app/src/main/res/values-ja/strings.xml`
- Create: `android/app/src/main/res/values-zh-rCN/strings.xml`
- Create: `android/app/src/main/res/values-ko/strings.xml`
- Create: `android/app/src/main/res/values-pt-rBR/strings.xml`
- Create: `android/app/src/main/res/values-it/strings.xml`

- [ ] перенести **677** переводимых строк, НЕ копируя 9 с `translatable="false"`
- [ ] перевести 4 `plurals`: `ja`/`zh-rCN`/`ko` — только `quantity="other"`
- [ ] не повторять давнюю неточность: `provider_apple`, `provider_google`, `provider_telegram` помечены `translatable="false"`, но лежат в каждой переведённой локали (оттого там 680 строк вместо 677) — в новые локали их класть не надо
- [ ] собрать релизный вариант: `ExtraTranslation` фатален в `lintVitalRelease`, это и есть проверка
- [ ] прогнать `LocalizationCatalogTest`

### Task 6: Порядок дат в Android

Реальная дыра форматирования — не деньги, а даты. iOS собирает формат через
`setLocalizedDateFormatFromTemplate` и порядок компонентов подхватит сам, а
Android жёстко задаёт шаблон `ofPattern("d MMMM yyyy")`: по-японски получится
«5 9月 2026» вместо «2026年9月5日». Деньги проверять почти нечего — `Money.swift`
принудительно ставит `maximumFractionDigits = 0` и подменяет символ валюты
своим, от локали остаётся только разделитель разрядов.

**Files:**
- Modify: `android/app/src/main/java/com/zagir/splitty/ui/groups/GroupsCommon.kt`
- Modify: `android/app/src/main/java/com/zagir/splitty/ui/profile/ProfileScreen.kt`
- Modify: тесты форматирования дат Android

- [ ] заменить `ofPattern` на `DateTimeFormatter.ofLocalizedDate` либо на шаблон из `DateTimeFormatterBuilder.getLocalizedDateTimePattern`
- [ ] написать тест на порядок компонентов для `ja` и `pt-BR`
- [ ] расширить андроидные тесты денег и дат: сейчас в них только `en` и `ru`
- [ ] решить, что делать с `uzbekSum` (`core/money/Money.kt:74-85`): он знает `ru` и `de`, всем остальным отдаёт английское «sum» — для пяти новых языков нужно либо слово на их языке, либо осознанное решение оставить латиницу
- [ ] проверить, что русский и английский форматы не изменились
- [ ] прогнать тесты Android

### Task 7: Передать язык клиента на сервер и локализовать вопросы AI

Промпт разбора расхода (`internal/ai/prompt.go`) целиком на русском, и модель
возвращает пользователю `questions` («Сколько стоила пицца?»), которые клиент
показывает как есть. `ParseInput` языка не содержит, клиент его не шлёт. Это
изменение контракта, а не правка промпта, поэтому идёт до заливки витрины.

**Files:**
- Modify: `internal/ai/ai.go`
- Modify: `internal/ai/prompt.go`
- Modify: `internal/rest/` (обработчик `/parse`)
- Modify: `ios/Splitty/Core/APIClient.swift`
- Modify: `android/app/src/main/java/com/zagir/splitty/core/network/`

- [ ] добавить язык в `ParseInput` и принять его в обработчике `/parse`
- [ ] дописать в промпт правило: `questions` писать на языке пользователя
- [ ] научить оба клиента слать язык интерфейса
- [ ] написать тесты сборки промпта с языком и обратной совместимости (язык не задан → прежнее поведение)
- [ ] прогнать тесты бэкенда и обоих клиентов

### Task 8: Локализовать push-уведомления

Самая заметная дыра: тела пушей собираются русскими литералами прямо в коде
(13 мест в `internal/bot/notifier.go`), напоминания о долге идут через
`i18n.Tr` но с языком из `DefineLang`, который знает только `ru`/`en`.

**Files:**
- Modify: `internal/bot/notifier.go`
- Modify: `conf/lang/` (или иной механизм ресурсов пушей)
- Modify: `internal/api/tg.go` (`DefineLang`)

- [ ] вынести 13 русских литералов пушей в ресурсы с ключами
- [ ] добавить локаль **в push-токен**, а не в профиль: `api.PushToken` хранит только `token` и `platform` (`internal/api/tg.go:237-241`), регистрация принимает те же два поля (`internal/rest/devices.go:42`). У человека может быть русский iPhone и английский iPad — язык у пуша устройства, а не аккаунта
- [ ] научить оба клиента слать локаль при регистрации устройства и обновлять её при смене языка системы
- [ ] `DefineLang` для этого не годится: он отдаёт только `ru`/`en` и означает язык БОТА
- [ ] перевести тексты пушей на 10 языков
- [ ] написать тест: пуш пользователю с японским языком собирается по-японски
- [ ] прогнать тесты бэкенда

### Task 9: Витрина App Store через существующий конвейер

**Files:**
- Create: `metadata/version/<следующая версия>/{ja,zh-Hans,ko,pt-BR,it}.json` — номер брать фактический, когда версия заведётся; каталоги в репозитории сейчас `1.4` и `1.9`
- Create: `metadata/app-info/{ja,zh-Hans,ko,pt-BR,it}.json`
- Modify: `ios/asc/push_metadata.py`

- [ ] создать файлы по образцу существующих локалей — тот же JSON, а не `.strings`
- [ ] перевести `description`, сохранив блок `SPLITOR PLUS` и обе обязательные ссылки (условия и политика): без них повторится отказ по 3.1.2(c)
- [ ] подобрать `keywords` заново под каждый магазин (не перевод), уложиться в 100 символов; `name` — бренд плюс пояснение в 30 символов, `subtitle` — 30
- [ ] параметризовать заливщик: `ios/asc/push_metadata.py:24` жёстко смотрит в `metadata/version/1.4`, хотя метаданные лежат уже для 1.9 — без этого скрипт зальёт содержимое старого релиза
- [ ] дописать пять локалей в список по умолчанию (`push_metadata.py:124`)
- [ ] залить в новую версию (НЕ в 1.8 и не в 1.9: первая продаётся, вторая на ревью) и сверить `asc localizations list`

### Task 10: Локализация подписок и группы подписок

Локализации подписок ревьюются вместе с версией, поэтому задача обязана попасть
в тот же релиз, что и Задача 9, а не «когда-нибудь после».

**Files:**
- Modify: настройки подписок в App Store Connect (через `asc`)

- [ ] завести локализации `Plus Monthly` и `Plus Yearly` на пяти новых языках (`name`, `description`)
- [ ] завести локализации **группы** `Splitor Plus` — её имя видно в системном шите и в «Управлении подписками»
- [ ] проверить лимиты полей
- [ ] сверить: у обоих продуктов должно стать 10 локалей
- [ ] завести названия базовых планов в Google Play Console — системный шит Play берёт имя оттуда, а не из ресурсов приложения

### Task 11: Витрина Google Play

**Files:**
- Create: `metadata/play/{ja-JP,zh-CN,ko-KR,pt-BR,it-IT}.json`
- Modify: `marketing/push-play-listing.py`

- [ ] создать пять JSON по образцу `metadata/play/en-US.json`
- [ ] дописать локали в словарь `LOCALES` (`marketing/push-play-listing.py:29-35`) — без этого файлы не подхватятся
- [ ] решить, какую feature graphic брать новым локалям: своя картинка или ссылка на `en-US` (сейчас в `metadata/feature-graphic/` только 5 PNG)
- [ ] помнить, что скрипт льёт только `title`, `shortDescription`, `fullDescription`; `whatsNew` через него не уходит вовсе
- [ ] проверить лимиты Play: короткое описание 80 символов — строже, чем в App Store

### Task 12: Русские строки, которых не видит ни один тест

Каталожные тесты — structural guard, а не приёмка: они сверяют НАБОРЫ ключей.
Строка, собранная как сырой `String`, в каталог не попадает и остаётся русской
на любом языке. Пример, найденный на ревью: `GroupsAddAction.title`
(`ios/Splitty/Features/Groups/GroupsListView.swift:19-23`) возвращает
`"Создать группу"` обычной строкой, и `Label(строка, systemImage:)` её не
локализует. iOS-сканер исходников ловит только `String(localized:)` и часть
конструкторов SwiftUI, причём второй regex — лишь по кириллице; `.navigationTitle`,
`.alert`, `.accessibilityLabel` и вычисляемые `String` не покрыты. Андроидный
`HardcodedTextTest` ищет тоже только кириллицу, поэтому английский литерал в
коде невидим обоим.

**Files:**
- Modify: `ios/Splitty/Features/Groups/GroupsListView.swift`
- Modify: `ios/Splitty/Features/Activity/ActivityView.swift`
- Modify: `ios/SplittyTests/LocalizationCatalogTests.swift`
- Modify: `android/app/src/test/java/com/zagir/splitty/HardcodedTextTest.kt`

- [ ] найти все места, где пользовательский текст собирается сырым `String`: начать с `GroupsAddAction.title` и `leaveConfirmTitle` (`ActivityView.swift:95-99`)
- [ ] перевести их на `LocalizedStringKey`/`String(localized:)` и добавить ключи в каталог
- [ ] расширить сканер исходников: `.navigationTitle`, `.alert`, `.accessibilityLabel`, `.accessibilityHint`
- [ ] расширить андроидный `HardcodedTextTest` — искать не только кириллицу
- [ ] прогнать оба клиента

### Task 13: Вычитка переводов по одной локали

**Files:**
- Modify: `ios/Splitty/Localizable.xcstrings`
- Modify: `android/app/src/main/res/values-*/strings.xml`

- [ ] пройти каждую локаль отдельным проходом: японский, китайский, корейский, португальский, итальянский
- [ ] проверить единый регистр вежливости внутри локали: ja — です/ます, ko — **하십시오체** (해요체 это вежливо-неформальный, для интерфейса магазина нужен формальный)
- [ ] собрать список предметных терминов **из каталога** (расход, доля, долг, группа) и проверить, что каждый переведён одинаково везде
- [ ] проверить строки с подстановками на осмысленность в контексте
- [ ] прогнать оба каталожных теста

### Task 14: Живая проверка на симуляторе

**Files:**
- нет изменений кода; находки чинятся правками каталогов

- [ ] пройти основные экраны на японском, китайском и корейском — искать обрезание и сломанные переносы
- [ ] отдельно проверить экран оплаты: там самая жёсткая вёрстка
- [ ] помнить, что имена тарифов на симуляторе берутся из `ios/Splitty/Splitty.storekit` (там только ru/en), а не из ASC — локализацию подписок симулятор не покажет
- [ ] проверить пустые состояния и тексты ошибок
- [ ] найденное завести как ➕ задачи и починить

### Task 15: Verify acceptance criteria

- [ ] пять языков присутствуют в обоих клиентах и обеих витринах
- [ ] подписки и группа подписок локализованы в десяти локалях
- [ ] пуш пользователю с японским языком приходит по-японски
- [ ] `GOTOOLCHAIN=go1.22.12 go build ./... && go vet ./...`
- [ ] `GOTOOLCHAIN=go1.24.6 go test ./internal/...`
- [ ] iOS: `xcodebuild test -scheme Splitty -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:SplittyTests`
- [ ] Android: `./gradlew :app:testDebugUnitTest` и релизный lint

### Task 16: [Final] Обновить документацию

- [ ] дописать в `CLAUDE.md` таблицу кодов локалей по трём системам — путаница `zh-Hans` / `values-zh-rCN` / `zh-CN` иначе повторится
- [ ] зафиксировать правило: новый язык добавляется в каталожные тесты ПЕРВЫМ действием, вместе с таблицей форм CLDR
- [ ] перенести план в `docs/plans/completed/`

## Post-Completion

**Ручная проверка:**
- покупка подписки на живом устройстве с японским языком: имя тарифа и цена в системном диалоге;
- карточка приложения в японской витрине — не обрезано ли описание.

**Внешние системы:**
- добавление `zh-Hans` в витрину **не открывает материковый Китай**: доступность страны настраивается отдельно, а для части приложений Apple требует номер ICP. Если целью был именно китайский рынок — это отдельная проверка, а не следствие перевода;
- страницы `/terms` и `/privacy` отдаются **только по-русски** (`internal/rest/legal.go:19`, `<html lang="ru">`, обработчики игнорируют запрос). Ссылки на них обязательны в описании по 3.1.2(c), и японец придёт на русскую страницу. Перевод — отдельная задача, но её стоит сделать до продвижения в новых странах;
- скриншоты магазина остаются английскими до первых установок (`asc-shots-pipeline`);
- отзывы теперь придут на языках, на которых в команде никто не читает.
