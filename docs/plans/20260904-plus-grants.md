# Plus по решению админа: гранты рядом со списком в окружении

4 сентября 2026.

> Ревизия 2. Первая версия прошла два независимых ревью — Codex и агент-ревьюер,
> состязательно. Оба нашли одно и то же: центральное утверждение «клиентов не
> трогаем» неверно **дважды**, и один из случаев — молчаливый. Плюс восемь
> находок, которых не было ни у меня, ни в первом круге. Таблица «находка → где
> закрыта» в конце.
>
> Заголовок тоже исправлен: `PLUS_COMP_USER_IDS` **не уходит** — та же переменная
> питает приём sandbox-чеков ревьюера магазина (`cmd/splitty/main.go:443-464`).
> Гранты встают рядом с ней, а не вместо.

## Overview

Надо уметь давать Plus людям из панели — друзьям, ранним пользователям, тем, у
кого что-то сломалось. Сейчас это невозможно: единственный способ — статический
список в окружении, то есть правка compose на сервере и перезапуск контейнера.

Тариф резолвится единственным местом — `Entitlements.Tier`
(`internal/service/entitlements.go:82`): comp-список → кеш → активные подписки.
Заводим третий источник — грант в базе, выдаётся из панели, всегда со сроком,
отзывается.

## Что оказалось неверным в первой версии

**«Клиентов не трогаем» — неправда, и по двум разным причинам.**

Первая, молчаливая. `ActiveByUser` **намеренно** возвращает истёкшие подписки
(`internal/repository/subscription.go:141-143`, комментарий прямо об этом), а
`writeSubscriptionState` (`internal/rest/subscription.go:311-322`) берёт
максимальную дату, ни разу не спросив `api.Subscription.Active`
(`internal/api/subscription.go:84-89`). Сегодня это безвредно: у просрочившего
`tier == free`, и экран уходит в бесплатную ветку. С грантом `tier == plus` — и
человек с прошлогодней отменённой подпиской увидит **«Активна до» с датой из
прошлого** и живую ссылку «Управлять подпиской» в стор, где ничего нет. Это
ровно та аудитория, которой гранты и раздают.

Вторая, крупнее. **На Android экрана подписки нет вообще.** В
`ui/profile/ProfileScreen.kt` ноль упоминаний Plus, тарифа и распознаваний
(проверено грепом); Plus всплывает только пейволом при исчерпании квоты
(`AddExpenseViewModel.kt:1261`). Друг с грантом на Android не увидит ничего,
кроме того, что пейвол перестал вылезать. Обоснование первой версии строилось
только на iOS-коде.

**Третье, помельче.** `SubscriptionStore.restore()`
(`ios/Splitty/Core/SubscriptionStore.swift:116-126`) возвращает `tier == .plus`
и подавляет «Покупок для восстановления не нашлось» при `tier == .plus`. Человек
с грантом и без покупок тапнет «Восстановить покупки» и получит молчаливый
успех. Android делает правильно: `return restored && isPlus`
(`billing/SubscriptionRepository.kt:131`).

## Context (from discovery)

- `internal/service/entitlements.go:82-107` — `Tier`: comp-список **до** кеша,
  кеш, подписки. Ошибка чтения возвращается наружу (fail closed, док-коммент
  `:74-81`), а `TierOrFree` (`:161`) разжалует до free. Кеш существует потому,
  что тариф считается на **каждом** распознавании (`:36-38`).
- `internal/service/entitlements.go:128` — `Invalidate(userId)` уже есть.
- Админский и публичный слушатели живут в **одном процессе** и делят один
  `Entitlements` — инвалидация кеша сработает без распределённой машинерии.
  (Появятся реплики — понадобится либо распределённая инвалидация, либо
  осознанное окно устаревания.)
- `internal/rest/admin.go:110-118` — мультиплексор, сегодня **только GET**.
  `adminAuth` (`:130-152`) — один общий токен на все возможности.
- `internal/rest/admin.go:301-314` — `handleAdminUser`: `Atoi` + `FindById` +
  404 на отсутствующего. Тот же guard нужен пишущим маршрутам.
- `internal/rest/admin_test.go:75-83` — `TestAdminRoutesAbsentFromPublicHandler`
  перечисляет админские пути; хелпер `doAdmin` (`:60-69`) захардкожен на GET.
- `internal/rest/delete_account.go:183-215` — `purgeUserData`, nil-очиститель =
  ошибка уже после tombstone.
- `cmd/splitty/main.go` — каждая коллекция зовёт `EnsureIndexes` явно
  (`:268-270` события, `:327-329` очередь пушей, `:394-396` подписки).
- `Makefile:4-7, 38-46` — `GOTOOLCHAIN=local`, свой go1.23.5, цели
  `make build/vet/test`. Скачивать тулчейн из сети запрещено намеренно.
- `docs/API.md:1043-1044` описывает `GET /me/subscription` полями покупки;
  админский раздел — с `:1144`.
- `~/projects/my/admin/internal/upstream/upstream.go:79` — у клиента панели
  единственный метод `Get`.
- `~/projects/my/admin/internal/auth/auth.go:40-49` — кука сессии `SameSite: Lax`,
  CSRF-токена нет.
- Android: `SubscriptionState` (`core/model/Models.kt:733-741`) несёт все нужные
  поля, `SubscriptionRepository.refreshSubscription()` их тянет. Данные есть —
  нет экрана.

## Решения, зафиксированные до кода

- **Грант всегда со сроком**, и срок ограничен сверху: `now < expiresAt <= now+2
  года`. Бессрочный никто не пересматривает, а опечатка `2226-01-01` — это тот же
  бессрочный, только незаметный.

- **Порядок источников: comp → кеш → покупки → гранты.** Гранты читаются
  **последними и только если Plus ещё не найден**. Две причины. Первая: подписка
  — частый случай, ранний выход экономит запрос на горячем пути. Вторая, важнее:
  при отказе коллекции грантов платящий подписчик **не должен** разжаловаться до
  free. Если Plus уже получен от покупки, до грантов дело не доходит вовсе;
  если не получен — ошибка чтения грантов возвращается наружу, как и ошибка
  подписок, и `TierOrFree` даёт free — тот же исход, что и раньше.

- **Победитель по дате, но реквизиты только от ЖИВОЙ покупки.**
  `writeSubscriptionState` выбирает бо́льшую дату из живой покупки и активного
  гранта, а `store` / `productId` / `manageUrl` / `autoRenew` заполняет **только**
  когда победила покупка, прошедшая `Active(now, slack)`. Иначе экран показывает
  дату из прошлого и ссылку в никуда.

- **Одна строка на живой грант, история не переписывается.** Продление — upsert
  по фильтру «живой грант этого человека» (`revoked_at` нет **и** `expires_at >
  now`). Живого нет — вставляется новая строка. Отзыв — `revoked_at`, не
  удаление. Так «продлили» и «выдали заново после отзыва» остаются разными
  фактами, а уникальность живого гранта держится фильтром, а не надеждой.

- **Отдельной коллекции аудита не будет.** Строка гранта уже несёт кто, когда,
  почему, когда отозван — и она append-only по предыдущему решению. Отдельный
  `admin_audit` с полем актора, которое всегда равно «panel», не отвечает **ни на
  один** вопрос, которого нет в гранте плюс строке лога `adminAuth`
  (`admin.go:148-152` пишет ip и путь каждого пропущенного запроса). Зато он
  создаёт нерешаемый спор: `target_user_id` — это данные человека, и по правилу
  проекта он обязан чиститься при удалении аккаунта, но тогда след умирает
  вместе с аккаунтом. Вместо коллекции: поле `revoked_reason` в гранте и явный
  `log.Info` с действием, целью и ip в обоих обработчиках.

- **Актор в гранте называется честно.** Не `granted_by` (знать, кто за токеном,
  неоткуда — у панели один пароль и один токен), а `source` со значением
  `panel`. Появится второй актор — появится и смысл в поле.

- **Пишущие маршруты — только POST и DELETE, никогда GET.** Кука панели
  `SameSite: Lax` не уходит на кросс-сайтовый POST, но уходит на top-level
  GET-навигацию. «Удобная ссылка» вида `GET …/plus/grant` открыла бы выдачу Plus
  по чужой ссылке.

- **Радиус общего токена принимается как есть.** Он уже отдаёт имена, суммы и
  долги всех людей; возможность выдать Plus — меньший ущерб, чем то, что он
  открывает сейчас. Отдельный токен на запись и лимитер не заводим: слушатель во
  внутренней сети, токен один. Записываем это решением, а не умолчанием.

- **Android получает свою строку Plus.** Иначе половина адресатов не увидит
  подарка вовсе. Строка минимальная: тариф и «Активна до», без ссылки «управлять»
  (у гранта её нет).

- **comp-список остаётся.** Он же включает приём sandbox-чеков ревьюера
  (`main.go:443-464`); удалить его — сломать ревью App Store.

## Development Approach

- каждая задача доводится до конца перед следующей, **включая проводку в
  `main.go`** — «собирается и тесты зелёные, а в проде не работает» задачей не
  считается;
- каждая задача содержит тесты;
- проверка: `make build && make vet && make test` (`Makefile:4-7` запрещает
  качать тулчейн из сети; свои `GOTOOLCHAIN=…` не выдумываем);
- перед задачей 1: `docker compose up -d mongo` — иначе тесты репозитория
  молча скипаются (`internal/repository/testsupport_test.go:41-60`);
- необязательные зависимости подключаются **сеттером**, а не параметром
  конструктора: `NewEntitlements` зовут из шести тестов плюс `main.go`, и третий
  позиционный параметр — семь правок ради ничего (конвенция репозитория:
  `internal/rest/server.go:306-308`).

## Testing Strategy

- **Резолв**: живой грант даёт Plus; истёкший и отозванный — нет; покупка и грант
  не мешают друг другу; **отказ коллекции грантов не разжалует платящего
  подписчика**.
- **Кеш**: после выдачи `Tier` возвращает Plus сразу, без ожидания TTL.
- **Состояние подписки**: истёкшая покупка + живой грант → дата гранта, `store` и
  `manageUrl` пустые; живая покупка + грант → реквизиты покупки.
- **Гранты**: повторная выдача при живом гранте продлевает ту же строку; выдача
  после отзыва создаёт новую, отозванная остаётся; срок в прошлом и срок дальше
  двух лет — 400; несуществующий или удалённый человек — 404.
- **Удаление аккаунта**: гранты вычищаются; тест падает без подключённого
  очистителя.
- **Публичный контур**: новые админские пути отсутствуют в `Handler()`
  (`admin_test.go:75-83`).
- **iOS**: «Восстановить покупки» у человека с грантом и без покупок честно
  говорит, что покупок нет.

## Задачи

### 1. Хранилище грантов

- [ ] `internal/repository/plus_grants.go`: коллекция `plus_grant`, поля
      `user_id`, `source`, `reason`, `revoked_reason`, `expires_at`,
      `revoked_at`, `created_at`
- [ ] `EnsureIndexes`: `{user_id, revoked_at}` — горячий путь резолва. Индекса
      по `expires_at` **нет**: коллекция на десятки строк, он ничего не даёт
- [ ] `LiveByUser` — имя не `ActiveByUser`: у подписок так называется метод,
      который истёкшие **возвращает** (`subscription.go:141-143`), и одно имя с
      двумя контрактами приведёт к фильтру в обоих местах или ни в одном.
      Срок режется здесь, в репозитории
- [ ] `Grant` — upsert по фильтру «живой грант» (`revoked_at` нет и
      `expires_at > now`), иначе вставка новой строки
- [ ] `Revoke`, `ListLive`, `DeleteByUserId` (реализация `userDataCleaner`)
- [ ] Тесты на живой mongo: продление, выдача после отзыва, истёкший не живой

### 2. Грант как третий источник Plus + проводка

- [ ] `Entitlements.SetGrants(reader)` — сеттер, nil = поведение как раньше
- [ ] `Tier`: гранты **после** подписок и только если Plus не найден
- [ ] `cmd/splitty/main.go`: `NewPlusGrantRepository(db)`, `EnsureIndexes` с
      `log.Warn` при ошибке, `entitlements.SetGrants(...)`
- [ ] Тесты: живой даёт Plus, истёкший и отозванный нет; **отказ грантов не
      трогает платящего подписчика**; попадание в кеш не читает гранты

### 3. Дата гранта доезжает до экрана подписки

- [ ] `writeSubscriptionState` (`subscription.go:300-329`): победитель по дате
      между **живой** покупкой (`Active(now, slack)`) и живым грантом
- [ ] `store` / `productId` / `manageUrl` / `autoRenew` — только от победившей
      живой покупки
- [ ] `Server.SetPlusGrants(...)` + проводка в `main.go`
- [ ] `docs/API.md:1043-1044`: контракт `GET /me/subscription` изменился —
      `tier: plus` может приехать с пустым `store` и датой из гранта
- [ ] Тесты: оба порядка (истёкшая покупка + грант; живая покупка + грант)

### 4. Удаление аккаунта чистит гранты

- [ ] `plus_grant` в `purgeUserData`, сеттер, безусловная проводка в `main.go`
- [ ] Фейк в `newDeleteSetup` (`delete_account_test.go:98-129`), обновить
      док-коммент `delete_account.go:156-182`
- [ ] Тест падает без подключённого очистителя

### 5. Админские маршруты

- [ ] `POST /admin/users/{userId}/plus` — тело `{expiresAt, reason}`,
      `expiresAt` в RFC3339, `now < expiresAt <= now+2 года`, `reason` ≤ 200
      символов (тела админского mux и так ограничены мегабайтом,
      `server.go:126`)
- [ ] `DELETE /admin/users/{userId}/plus` — тело `{reason}`, необязательное
- [ ] Оба: `Atoi` + `FindById` + 404 на отсутствующего и удалённого — как
      `handleAdminUser` (`admin.go:301-314`). Опечатка в тринадцатизначном
      номере иначе создаёт грант в никуда, молча и с 200 OK
- [ ] Оба зовут `Entitlements.Invalidate(userId)` — иначе Plus появляется не
      сразу, и это выглядит поломкой
- [ ] Оба пишут `log.Info` с действием, целью и ip
- [ ] `GET /admin/plus` — кому сейчас выдано
- [ ] `GET /admin/users/{userId}` дополняется блоком
      `plus: {tier, source: purchase|grant|comp, expiresAt, reason}` — без него
      панель не отличит «платит» от «подарено» и от comp-списка
- [ ] `docs/API.md`, админский раздел (`:1144`)
- [ ] Новые пути — в `TestAdminRoutesAbsentFromPublicHandler`; хелпер `doAdmin`
      расширяется под POST/DELETE с телом
- [ ] Тесты: 401 без токена, 400 на срок в прошлом и дальше двух лет, 404 на
      несуществующего, повтор продлевает

### 6. Панель: выдать и отозвать

*(репозиторий `~/projects/my/admin`)*

- [ ] `upstream.Client` умеет только `Get` (`upstream.go:79`) — нужен метод с
      телом и произвольным методом
- [ ] Прокси `POST` / `DELETE /api/projects/{id}/users/{userId}/plus` —
      **первое пишущее действие панели**. Только POST и DELETE, никогда GET
      (кука `SameSite: Lax`, CSRF-токена нет)
- [ ] На карточке человека: состояние Plus из нового блока, «Выдать» с выбором
      срока и полем причины, «Отозвать» с подтверждением
- [ ] Блок «Кому выдан Plus»
- [ ] Смотреть глазами: выдача, отзыв, состояние до и после

### 7. iOS: восстановление покупок перестаёт врать

- [ ] `SubscriptionStore.restore()` (`SubscriptionStore.swift:116-126`) —
      возвращать факт восстановления, а не `tier == .plus`, как на Android
      (`billing/SubscriptionRepository.kt:131`)
- [ ] Тест: у человека с грантом и без покупок «Восстановить» честно говорит,
      что покупок нет

### 8. Android: строка Plus в профиле

- [ ] Секция в `ui/profile/ProfileScreen.kt`: тариф и «Активна до» из
      `SubscriptionState` (`core/model/Models.kt:733-741`), данные уже тянет
      `SubscriptionRepository.refreshSubscription()`
- [ ] Ссылки «управлять» нет, когда нет `manageUrl` — у гранта его не бывает
- [ ] Строки в пять локалей (`values`, `values-en`, `values-es`, `values-fr`,
      `values-de`)
- [ ] Перезаписать эталоны Roborazzi: CI гоняет `verifyRoborazziDebug` и следом
      `git diff --exit-code`, иначе сборка красная
- [ ] Событие `screen_view` на экране уже есть — новых не нужно

### 9. Прод

- [ ] Выкатка бэкенда и панели
- [ ] Проверить на живом человеке: выдать, увидеть Plus **на обеих
      платформах**, отозвать
- [ ] Убедиться, что у платящего подписчика ничего не изменилось

## Находка → где закрыта

| Находка | Кто | Где закрыта |
|---|---|---|
| Истёкшая покупка маскирует грант: дата из прошлого и мёртвая ссылка «управлять» | оба, high/critical | Решения + задача 3 |
| На Android экрана подписки нет вообще — грант не виден никак | оба, high | Задача 8 |
| `upsert`-продление противоречит «отзыв — не удаление» | агент, critical | Решения: фильтр «живой грант» + задача 1 |
| Коллекция аудита противоречит правилу чистки при удалении аккаунта | оба, critical/medium | Решения: коллекции не будет |
| `EnsureIndexes` новой коллекции нигде не вызывается | агент, critical | Задачи 1 и 2 |
| Проводки в `main.go` нет ни в одной задаче до последней | агент, important | Задачи 2, 3, 4 |
| Не определено поведение при отказе чтения грантов: платящий разжалуется до free | Codex, high | Решения: порядок источников + тест |
| Нет проверки существования человека — грант в никуда, молча | оба, high/important | Задача 5 |
| Первое пишущее действие за общим read-токеном | оба, high | Решения: принято явно, с обоснованием |
| `GET /admin/users/{id}` не отдаёт состояние Plus — панель не отличит источники | агент, important | Задача 5 |
| Параметр конструктора вместо сеттера ломает 6 тестовых файлов | агент, important | Development Approach |
| Команды сборки не те, что в `Makefile` | агент, important | Development Approach |
| Порядок задач 5↔6 неисполним (аудит до обработчиков) | агент, important | Снят вместе с коллекцией аудита |
| iOS «Восстановить покупки» врёт человеку с грантом | агент, minor | Задача 7 |
| Клиент панели умеет только `Get` | агент, minor | Задача 6 |
| CSRF: писать только POST/DELETE | агент, minor | Решения + задача 6 |
| `docs/API.md` — меняется и клиентский контракт, не только админский | агент, minor | Задачи 3 и 5 |
| Формат и границы `expiresAt` не заданы | оба, minor | Решения + задача 5 |
| Семантика «активный» разойдётся с подписками | агент, minor | Задача 1: метод `LiveByUser` |
| Заголовок врёт: comp-список не уходит, он же питает sandbox-чеки | оба, minor/medium | Заголовок и решения |
| Индекс `{expires_at}` не нужен на коллекции в десятки строк | агент, переусложнение | Задача 1 |
| `granted_by` будет константой | агент, переусложнение | Решения: поле `source` |
| Тесты репозитория молча скипаются без mongo | агент | Development Approach |
| Кеш работает, потому что слушатели в одном процессе | Codex | Context, с оговоркой про реплики |
