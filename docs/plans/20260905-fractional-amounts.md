# Дробные суммы: 20.8 $ в расходах и долгах

## Overview

Сейчас в продукте нет копеек и центов: сумма — целое число единиц валюты.
Решение осознанное и записано в трёх местах (`docs/API.md:10`,
`ios/Splitty/Core/Money.swift:26`, `android/app/src/main/java/com/zagir/splitty/core/money/Money.kt:11`), а
опирается на него всё: модель (`Sum int`, `Price int`), деление
(`api.ShareOf` — целочисленное), форматирование на обоих клиентах, тексты бота
и пушей, схема ответа AI.

Задача: разрешить дробные суммы там, где они есть в жизни — «ужин 20.8 $»,
«с тебя 6.93 €» — и не разъехаться в копейке при делении на троих.

**Float в деньгах у нас уже есть — в трёх местах.** Доли получателей
(`RecipientWithSum.Sum float64`), долговой движок
(`internal/service/service.go:288-341`) и статистика
(`GetUserCostsSum`, `:524`). Целые единицы снаружи держатся округлением на
границе API. То есть план не добавляет дроби в систему, а наводит в них
порядок: единица становится одна и целая, а float уходит отовсюду. Это
главный побочный выигрыш и главный же риск — см. Задачу 8.

Это не значит, что работа лёгкая: ввод дробей появляется у людей, поля
контракта новые, а миграция трогает все документы комнат. Ниже два десятка
мест, помеченных ⚠️, — это и есть цена.

**Заодно, тем же заходом — Задачи 15 и 16.** Дробей они не касаются, но живут
на тех же двух экранах: создание тусы и её настройки. Сегодня создание
спрашивает единственное, что человек уже никогда не исправит (название
переименовать нельзя нигде), и молчит про валюту, которую потом можно сменить
только с потерей смысла сумм. Возвращаться в эти экраны дважды дороже, чем
сделать сразу.

**Хранить дробное как дробное нельзя.** Деньги во float — это накопленная
ошибка: 0.1 + 0.2 ≠ 0.3, и сумма долей перестаёт сходиться с суммой расхода
ровно там, где человек это заметит. Проверенный способ — хранить
МИНОРНЫЕ единицы целым числом (центы, копейки), а дробь показывать при
выводе. Тогда всё нынешнее целочисленное деление остаётся верным как есть:
меняется только цена деления.

## Context (from discovery)

- Модель: `internal/api/service_models.go:43` (`Operation.Sum`), `:99`
  (`OperationItem.Price`), `:143` (`Debt.Sum`) — везде `int`.
- Деление: `internal/api/money.go` (`ShareOf`), `internal/api/itemsplit.go`
  (`SplitItem`, `SplitSurcharge`) — целочисленная арифметика с раздачей
  остатка. Ломать её не надо, надо сменить единицу.
- Форматирование: `ios/Splitty/Core/Money.swift`,
  `android/app/src/main/java/com/zagir/splitty/core/money/Money.kt`, `internal/api/currency.go:37`
  (`MoneyWithSymbol` — бот и пуши).
- AI: `internal/ai/prompt.go:79-89` — схема ответа объявляет `integer`.
- Бот: `internal/bot/operation_screen.go:403` — `strconv.Atoi` на вводе суммы.
- Клиенты: `Long`/`Int` в моделях (`android/app/src/main/java/com/zagir/splitty/core/model/Models.kt`,
  `ios/Splitty/Core/Models.swift`).
- Затронуто: 16 файлов Go, 9 iOS, 11 Android, 21 файл тестов.

## Development Approach

- **testing approach**: обычный — код, потом тесты в той же задаче.
- «Зелёные тесты» — это конкретные команды из Задачи 17, а не «прогнать всё».
  У задачи, тронувшей только Go, это Go; тронувшей клиент — команда клиента.
- Правило единицы: **в модели и в арифметике — только минорные единицы**.
  Дробь появляется в двух местах: при вводе (разбор строки) и при выводе
  (форматирование). Между ними её нет.

## Solution Overview

1. **Шкала показа у ТУСЫ; валюта задаёт умолчание.** См. «Принятые решения».
   Ниже — про то, как эта шкала устроена и почему не называется ISO.

   **Шкала показа, а не «минорная единица ISO».** У иены и воны
   дробной части не бывает вовсе, у доллара и евро — два знака. Живёт в
   справочнике (`api.Currencies`), а не в форматтерах: две копии разошлись бы
   молча.

   Называть поле `displayExponent`, а не `minorUnit`: у рупии, сума и тенге по
   ISO 4217 два знака, а мы сознательно берём ноль (см. Задачу 1). Имя,
   обещающее ISO, однажды заставит кого-то «исправить» справочник.

   У шкалы обязана быть ВЕРСИЯ (`scaleVersion`). Без неё после смены
   экспоненты `2080` не отличить от «2080 рупий при нуле» и «20.80 при двух»,
   а клиентская очередь и кеш могут прислать запись, записанную по прежней
   шкале.
2. **Новое поле рядом со старым.** `sum` остаётся целыми единицами для сборок,
   которые уже стоят у людей, `sumMinor` несёт точное значение. Смена единицы
   в существующем поле показала бы старому клиенту 120 000 ₽ вместо 1 200 ₽ —
   ровно в день выкатки и у всех сразу.

   Честная цена: старый клиент увидит округлённые числа, и на дробных
   расходах они не равны настоящим. Инвариант «сумма долей равна сумме» при
   этом сохраняется — им занимается проекция из следующего пункта, — но
   расход 20.8 на троих старая сборка покажет как 21 с долями 7/7/7.
3. **Старое поле — не `round` каждого числа по отдельности, а ОДНА проекция.**
   Округлив сумму и доли независимо, получим 20.20 → сумма 20 при долях
   7/7/7: старый инвариант «Σ долей = сумма» сломается. Проекция округляет
   сумму и раздаёт разницу по долям тем же правилом, что и деление.

   Отсюда же: проверка «пришли оба поля и не сходятся» сравнивает старое
   поле с ПРОЕКЦИЕЙ минорного, а не с точным равенством. И расход меньше
   половины единицы даёт в старом поле ноль — значит «старая сборка не
   ломается» верно только при запрете сумм меньше 0.5 в новой.

4. **Старый клиент не должен затирать копейки.** Правка операции заменяет
   встроенный документ ЦЕЛИКОМ (`repository.go:795`, `$set operations.$`).
   Старая сборка прочитает округлённое, отправит PUT — и центы исчезнут.
   Нужен отказ `upgrade_required` на правку дробной операции старым клиентом.

5. **Новый клиент не должен молча терять копейки на старом сервере.**
   `decodeJSON` не запрещает неизвестные поля (`handlers.go:49`), поэтому
   старый бэкенд примет `sumMinor` и сохранит расход без дробной части.
   Дробную запись слать ТОЛЬКО минорным полем, без старого: тогда старый
   сервер откажет, а не соврёт.

6. **Арифметика не меняется.** `ShareOf` и `SplitItem` остаются теми же —
   целые числа, раздача остатка. Меняется цена деления: остаток в копейку, а
   не в рубль.
7. **Округление одно на всех и вниз по остатку.** Правило уже реализовано и
   описано в `docs/API.md:20`; переносим как есть.

## Technical Details

- `CurrencyInfo` получает `displayExponent` и `scaleVersion`. Таблица шкал — в
  Задаче 1; она же единственное место, где это решение записано.
- API: у операции появляются `sumMinor`, у позиции `priceMinor`, у долга
  `sumMinor`. Старые поля собирает ЕДИНАЯ проекция (Решение 3), а не
  независимое округление каждого числа.
- Хранение: `sum_minor`, `price_minor` в mongo; миграция считает их по валюте
  комнаты, а доли пересчитывает делением, а не умножением (Задача 6).
- Ввод: клиент разбирает строку с одним разделителем, группировка запрещена, и
  шлёт минорные единицы. Больше знаков, чем у шкалы, — ошибка ввода.
- AI: ответ модели разбирается точно (`json.Number`), а не через `float64`:
  после float «20.8» и «20.80» уже неразличимы.

## Принятые решения

**Шкала — свойство ТУСЫ, а не валюты.** Валюта задаёт значение по умолчанию,
человек может переключить в настройках тусы. Так копейки достаются тем, кому
они нужны, а рублёвая туса живёт как жила.

Умолчания по валютам:

| без копеек | с копейками |
| --- | --- |
| RUB, KZT, UZS, IDR | USD, EUR, CNY, BRL |
| JPY, KRW — переключателя нет вовсе: минорной единицы не существует | |

**Включение копеек разрешено всегда**, это точный пересчёт вверх: 1200 → 120000,
ничего не теряется. **Выключение — с явным предупреждением**: 20,80 станет 21,
и человек должен это подтвердить.

Что это упрощает: миграция трогает только тусы, у которых шкала стала
двойкой, — доллар, евро, юань, реал. На проде это 8 комнат из 104, а не 86.

**Старой сборке объясняем только в момент отказа на правку** — «обновите
приложение, чтобы менять расходы с копейками». Отдельной полоски в тусе не
заводим: человек упирается в стену ровно один раз, и там же узнаёт причину.
Смотреть тусу он продолжает как раньше, с округлёнными числами.

**Дробный ввод включается признаком в ответе сервера.** Клиент выходит один
раз и спрашивает, можно ли дроби; включение и откат — без похода в магазин.
Запрет живёт на СЕРВЕРЕ и отвергает дробные значения на всех входах разом —
расход, погашение, позиции чека, разбор AI, бот, — а не прячет клавишу в
приложении. Уже сохранённые дроби при выключенном признаке продолжают
читаться и показываться точно.

**При неровном делении человек видит СВОЮ долю точно**, а не разброс: «ваша
доля 6,94 $». Диапазон «6,93–6,94» с копейками появлялся бы почти в каждом
расходе и перестал бы что-либо значить, а своё число человеку и нужно. Там,
где долю показывают не себе, а списком по людям, у каждого стоит его точная.

**В тусе с копейками дробная часть показывается ВСЕГДА**, даже когда она
нулевая: «20,80 $» и «1 200,00 $». Столбик чисел получается ровным, разряды
совпадают, и сумму нельзя прочесть не в том порядке. Правило одно на всё:
экраны, пуши, тексты бота. В тусе без копеек ничего не меняется.

## Implementation Steps
### Task 1 (фаза 1): Шкала тусы и умолчания валют
**Files:**
- Modify: `internal/api/currency.go`
- Modify: `internal/rest/handlers.go` (справочник в ответе)
- Modify: `ios/Splitty/Core/Models.swift`, `android/app/src/main/java/com/zagir/splitty/core/model/Models.kt`

- [ ] `displayExponent` у КОМНАТЫ (хранится в документе), у валюты в
      справочнике — умолчание для новой комнаты
- [ ] таблица умолчаний: ноль у RUB, KZT, UZS, IDR, JPY, KRW; двойка у USD,
      EUR, CNY, BRL. У иены и воны переключателя нет: минорной единицы не
      существует
- [ ] имя именно `displayExponent`: `Exponent` или `minorUnit` обещали бы
      соответствие ISO, которого у нас нет
- [ ] ⚠️ **решить один раз: ISO или практика.** По ISO 4217 у рупии, сума и
      тенге экспонента 2, но минорной единицы в обороте нет — и на живых данных
      суммы в IDR доходят до 100 000 000, то есть в минорных станут 10
      миллиардами без всякой пользы. Предлагаю практическую: 0 у JPY, KRW, IDR,
      UZS, KZT; 2 у RUB, USD, EUR, CNY, BRL. Поменять потом можно только
      миграцией всех сумм этой валюты
- [ ] клиенты читают экспоненту из справочника, а не хранят свою копию
- [ ] **где живёт версия шкалы**: `scaleVersion` растёт при любом изменении
      экспоненты любой валюты. ⚠️ Конвертом сверху его отдать НЕЛЬЗЯ:
      `GET /currencies` возвращает голый массив, и оба клиента декодируют
      список (`handlers.go:537`, `APIClient.swift:712`,
      `SplittyApi.kt:229`) — конверт сломал бы установленные сборки. Класть
      в КАЖДУЮ строку справочника, тестом проверять, что во всех строках она
      одинаковая
- [ ] ⚠️ **сервер обязан помнить прежние таблицы шкал**, а не только текущую:
      номера версии мало, по нему нужно узнать, какая экспонента была у валюты
      тогда. Хранить историю `(версия, валюта, экспонента)` в коде
- [ ] тест перехода: запись, сделанная при версии N, принимается сервером с
      версией N+1 и даёт ту же сумму
- [ ] признак `fractionalInput` рядом со справочником, необязательное и ложное по умолчанию — старый сервер его
      не отдаёт, и клиент обязан прочесть это как «дроби нельзя»
- [ ] тем же тестом: значение одинаково во всех строках справочника
- [ ] ⚠️ **запрет ПИСАТЬ дроби живёт на сервере**, а не в клавиатуре: проверка
      на входах расхода, погашения, позиций, разбора AI и бота
- [ ] матрица тестов на КАЖДОМ из этих входов, иначе «накрывает все» останется
      проверенным на одном обработчике:

      | вход | флаг выключен | флаг включён |
      | --- | --- | --- |
      | расход | целое принято, дробное отвергнуто | дробное принято |
      | погашение | то же | то же |
      | цена позиции и доля | то же | то же |
      | разбор AI | то же | то же |
      | бот | то же | то же |

- [ ] и в обоих состояниях флага чтение отдаёт уже сохранённое дробное значение
      точно: выключенный флаг запрещает писать, а не портит записанное
- [ ] тест: у каждой валюты справочника экспонента задана и совпадает с
      принятой таблицей (не с ISO — расхождение осознанное, и тест обязан
      падать, если кто-то «поправит» его обратно по справочнику ISO)
- [ ] прогнать три стороны

### Task 2 (фаза 1): Совместимая модель — оба представления рядом
**Files:**
- Modify: `internal/api/service_models.go`, `internal/api/money.go`,
  `internal/api/itemsplit.go`
- Modify: `internal/repository/repository.go` (чтение/запись операций)

- [ ] добавить `SumMinor`/`PriceMinor`, старые поля объявить производными
- [ ] ⚠️ **отличать «поля нет» от «ноль»**: у немигрированного документа
      `SumMinor` разберётся в ноль и будет неотличим от честного нуля.
      Указатель или свой разбор — обязателен, особенно для `ItemShare.Amount`,
      где ноль осмыслен
- [ ] тип денег — `int64` (или свой `MoneyMinor`), не `int`: потолок в
      миллиард единиц при шкале 2 это уже 10^11, а доли умножаются на вес
- [ ] ⚠️ **`OperationDiff.RecipientShareChange.OldSum/NewSum` — тоже `float64`**
      (`service_models.go:190`): через них идут тексты уведомлений об
      изменении доли. Обещание «float уходит отовсюду» обязано накрыть и их
- [ ] ⚠️ **`RecipientWithSum.Sum` — это `float64`** и в модели
      (`service_models.go:67`), и в mongo. Доли получателей лежат дробными уже
      сейчас, и API приводит их к целым на выходе. Перевести в минорные целые:
      иначе float остаётся ровно там, где считаются деньги человека
- [ ] читать документы обоих видов и писать оба представления; арифметика
      пока СТАРАЯ — переключение единицы это Задача 7 (фаза 3)
- [ ] тест: документ без минорных полей читается и пишется обратно без потерь

### Task 3 (фаза 1): Переключатель копеек в настройках тусы

**Files:**
- Modify: `internal/rest/handlers.go`, сервис комнаты
- Modify: настройки тусы на обоих клиентах и в боте

- [ ] включение копеек: точный пересчёт всех сумм тусы вверх (сумма, доли,
      цены позиций, `shares[].amount`, погашения, архив и черновики)
- [ ] выключение: округление вниз по тому же правилу, что и деление, и явное
      подтверждение человеком — «20,80 станет 21»
- [ ] пересчёт атомарен: половина тусы в одной шкале, половина в другой — это
      деньги, которых нет
- [ ] ⚠️ **на чужих телефонах могут лежать неотправленные расходы в прежней
      шкале** — снимок шкалы в записи очереди и отказ при несовпадении
      (Задача 10), а не пересчёт
- [ ] у иены и воны переключатель не показывается
- [ ] тесты: включили — суммы те же на вид; выключили — округлились; очередь с
      прежней шкалой отвергнута с понятной ошибкой

### Task 4 (фаза 1): Смена валюты комнаты
**Files:**
- Modify: `internal/rest/handlers.go` (`handleUpdateCurrency`, `:500`)
- Modify: сервис смены валюты — бот меняет её мимо REST
  (`internal/bot/setting_screen.go:41` → `rs.UpdateCurrency`)

⚠️ **Блокер.** Валюту комнаты меняют одним запросом, и проверяется только то,
что код есть в справочнике. Операция при этом валюты не хранит — она у
комнаты. Смени доллар на иену, и записанное `2080` (20.80 $) станет читаться
как 2080 иен: деньги вырастут в сто раз, не изменившись в базе ни на единицу.

Сегодня это безопасно только потому, что у всех валют одна шкала.

- [ ] запретить смену между валютами с РАЗНОЙ шкалой, если в комнате есть хоть
      одна операция — включая архивные, черновики и погашения
- [ ] ⚠️ **проверка живёт в сервисе, а не в обработчике REST**: валюту меняет и
      бот (`internal/bot/setting_screen.go:41`), и защита в `handlers.go`
      его не остановит
- [ ] разрешать смену внутри одной шкалы, как сейчас
- [ ] ⚠️ «пустая комната» на сервере не значит «пустая везде»: на чужом
      телефоне может лежать неотправленный расход в прежней валюте. Отсюда
      снимок валюты в очереди и отказ вместо пересчёта — Задача 10
- [ ] сообщение объясняет причину, а не «нельзя»
- [ ] тесты: доллар→евро можно, доллар→иена нельзя, доллар→иена в пустой
      комнате можно

### Task 5 (фаза 1): API и документация контракта
**Files:**
- Modify: `internal/rest/handlers.go`, `internal/rest/parse_handler.go`
- Modify: `docs/API.md`

- [ ] приём минорных полей на создании и правке расхода — не только `sumMinor`,
      но и `recipientSums`, цены позиций и `shares[].amount`; старые поля
      принимаются, пока живы прежние сборки
- [ ] отказ, если пришли оба и они не сходятся. **Сходятся — это равенство
      старого поля ПРОЕКЦИИ минорного** (Решение 3), а не `старое == минорное/100`
- [ ] минимальная сумма расхода — половина единицы валюты: меньше даёт в
      старом поле ноль. Сейчас порог `req.Sum < 1` (`handlers.go:611`), его и
      заменить
- [ ] правка дробной операции без полного набора минорных полей — отказ,
      независимо от того, кто прислал запрос. Текст объясняет причину:
      «обновите приложение, чтобы менять расходы с копейками» (на десяти
      языках, см. Задачу 13). ⚠️ **Решать по ТЕЛУ, а не по
      `X-Client-Version`**: заголовок у нас прямо объявлен «ramp
      совместимости, а НЕ граница безопасности» (`quota.go:32`), его можно не
      слать, и бот через этот обработчик вообще не ходит. Версия годится
      только на текст ошибки и телеметрию
- [ ] правило клиента, записанное явно: ЦЕЛЫЙ расход шлётся обоими полями,
      ДРОБНЫЙ — только минорным. Так старый сервер откажет на дробном и
      примет целый, а не сломается на всём подряд
- [ ] пришли только минорные — сервер САМ пишет округлённое старое поле в
      базу: старый клиент читает ту же комнату и обязан увидеть в ней число, а
      не пустоту
- [ ] переписать раздел про деньги в `docs/API.md`: единица, экспонента,
      правило округления, срок жизни старого поля
- [ ] тесты REST на оба поля и на их расхождение

### Task 6 (фаза 2): Миграция
**Files:**
- Modify: `internal/repository/migrate.go`
- Modify: `cmd/splitty/main.go`

- [ ] миграция проставляет `sum_minor = sum * 10^exp` по валюте комнаты
- [ ] ⚠️ **у 86 комнат из 104 валюты нет вовсе** (поля нет, `null` или пустая
      строка) — на проде это 83% данных. Такие комнаты считать `RUB`
      (`api.DefaultCurrency`), иначе большая часть базы останется в старой
      единице, и разъезд будет тихим
- [ ] маркер и узкий фильтр, как у прежних миграций; повтор безвреден
- [ ] ⚠️ **маркера мало.** Откатись сервер после миграции — старый код при
      первой же правке сотрёт минорные поля документа (`$set operations.$`), а
      следующий новый запуск увидит маркер и дырку не залатает. Поэтому
      КРОМЕ разовой миграции нужен дешёвый проход на каждом старте: найти
      документы с суммой, но без минорных полей, и достроить их. Пустой
      результат — обычное состояние. Это скан коллекции: запрос по
      отсутствующему вложенному полю индексом не покрывается. На нынешних 104
      комнатах это доли секунды, но обещать «дёшево» на порядок большем объёме
      нельзя — тогда понадобится отдельный флаг на документе
- [ ] старое поле `sum` продолжает отдаваться округлённым — проверить тестом,
      что сборка без поддержки копеек видит те же числа, что видела вчера
- [ ] ⚠️ **у валют с нулевой экспонентой умножать не на что** — JPY, KRW и (по
      принятой таблице) IDR, UZS, KZT уже хранятся в своей единственной
      единице. На проде это заметная доля: рупиевые комнаты есть, и суммы там
      восьмизначные
- [ ] мигрировать и `recipients_with_sum`: доли лежат отдельно от суммы
      операции, и накрыть надо оба
- [ ] ⚠️ **доли пересчитываются, а не умножаются**: у старого «поровну» 100/3
      лежит 33.33 у каждого, и поэлементное умножение даст 9999 вместо 10000.
      Для равного деления и легаси-получателей считать заново через `ShareOf`
      от суммы операции; для точных и позиционных — переносить значение и
      проверять, что сумма сошлась
- [ ] ⚠️ **перечислить объём явно**: активные операции, архив, черновики,
      погашения, легаси-`recipients` без долей и `shares.amount` в позициях
      чека. Архив виден в админке и участвует в разборе жалоб
- [ ] тест на реальном mongo: до миграции, после, повтор без маркера
- [ ] тест: комната без валюты мигрирует как рублёвая

### Task 7 (фаза 3): Внутренняя арифметика на минорных
**Files:**
- Modify: `internal/api/money.go`, `internal/api/itemsplit.go`

Отдельно от Задачи 2 намеренно: переключить арифметику до миграции значит
считать по немигрированным документам. Исполнитель, идущий по плану сверху
вниз, обязан пройти миграцию раньше.

- [ ] перевести `ShareOf`, `SplitItem`, `SplitSurcharge` на минорные единицы
- [ ] сумма долей обязана совпадать с суммой расхода ДО копейки — расширить
      `TestShareOfSumsToTotal` на дробные случаи (100.01 на троих)
- [ ] тест разбора: строка «0.1» даёт 10 минорных, «0.10» — тоже 10, «0.100»
      отвергается
- [ ] тесты на переполнение во всех путях деления
- [ ] прогнать бэкенд

### Task 8 (фаза 3): Долги, погашение и статистика
⚠️ Долговой движок сегодня считает во `float64` и усекает в конце
(`internal/service/service.go:288-341`, `calculateUserBalance:467`). Переход на
целые минорные единицы убирает оттуда float — это и есть выигрыш, — но точки
округления сместятся, и существующий долг может измениться на единицу. Это
видно людям: «мой долг был 1 200, стал 1 199».

**Files:**
- Modify: `internal/service/*` (расчёт долгов), `internal/rest/handlers.go`

- [ ] взаимозачёт долгов в минорных единицах; сумма встречных долгов сходится
- [ ] погашение принимает дробную сумму и не даёт отдать больше долга
- [ ] графики и «Итоги» считают в минорных, показывают дробью
- [ ] ⚠️ **деньги есть во всех сводных DTO**: `totalSpent`, `mySpent`,
      `myBalance`, балансы друзей, повалютные итоги, ряды по дням и месяцам,
      суммы участников, топ операций и админский API (`internal/rest/dto.go`,
      `handlers.go:1289`, `admin.go:66`). Каждому нужна пара «точное минорное
      плюс округлённое старое», иначе старый клиент увидит пустоту
- [ ] ⚠️ **суммы разных валют нельзя сравнивать числом**: напоминания выбирают
      «самую крупную» комнату (`reminders/reminders.go:138`), а клиенты
      сортируют валюты по модулю (`Money.swift:116`, `Money.kt:108`). При
      разных шкалах 10000 центов сравнивается с 5000 рупий. Решить: не
      ранжировать разные валюты по сумме вовсе
- [ ] ⚠️ **пороги «единица» в движке НЕ умножать на сто.** В `service.go`
      шесть мест сравнивают деньги с единицей: `:337`, `:345`, `:370`
      (`b > 1 || b < -1`), `:456`, `:498`, плюс `moneyToInt:547` с его
      допуском `1e-6`. Часть из них — не бизнес-правило, а ЗАПЛАТКА НА
      FLOAT: у порога `:370` прямо написано, что он прячет «до рубля пыли»
      от усечения долей. На точных минорных единицах пыли не остаётся —
      `ShareOf` раздаёт остаток целиком, — и такие пороги обязаны стать
      `> 0`. Механический перевод в `> 100` проглотит настоящий долг в
      рубль и покажет «никто никому не должен» там, где должны
- [ ] разобрать КАЖДЫЙ из шести по отдельности и записать в коде, чем он
      стал: порогом «есть ли долг вообще» (`> 0`) или порогом показа
      (тогда откуда взялось значение). `moneyToInt` после перехода
      исчезает вовсе — округлять больше нечего
- [ ] тест на каждый: долг ровно в одну минорную единицу доживает до
      ответа, а не отсеивается порогом
- [ ] тест: долг 6.93 € гасится ровно, остатка 0.001 не возникает
- [ ] **сверка на копии живых данных**: посчитать долги всех комнат до и после
      перехода и выписать все расхождения. Пустой список — критерий приёмки;
      непустой — разбирать каждое, а не списывать на округление

### Task 9 (фаза 3): Выгрузка расходов наружу
**Files:**
- Modify: `internal/dailyexpenses/integration.go`

Раз в минуту сервис сериализует `[]api.Operation` целиком и шлёт на сторонний
адрес (`:107`). Это доменная модель из mongo, отданная наружу как есть, и у
неё нет ни комнаты, ни валюты (`service_models.go:35-63`) — в одном массиве
смешаны операции разных комнат с разными валютами. Получатель уже сегодня не
может истолковать число: он не знает, в чём оно.

Смена единицы делает это опасным: то же поле начнёт значить другое, а
предупредить получателя нечем.

- [ ] перестать отдавать доменную модель: свой DTO с версией
- [ ] в конверте — комната, валюта, шкала и версия шкалы
- [ ] пара «точное минорное плюс округлённое старое», как в API
- [ ] тест на точный JSON: поля и их порядок закреплены, случайная правка
      модели его роняет
- [ ] предупреждение получателя — шаг человека, см. Post-Completion

### Task 10 (фаза 4): Офлайн-очередь переживает обновление
**Files:**
- Modify: `ios/Splitty/Core/OutboxStore.swift`, `android/app/src/main/java/com/zagir/splitty/data/OutboxStore.kt`

Расход, введённый без сети, лежит на диске телефона (`sum: Int` на iOS,
`sum: Long` на Android) и уходит на сервер потом. Обновись приложение в этом
промежутке — запись отправится в старых единицах, и человек потеряет свои же
деньги в сто раз.

- [ ] в записи очереди лежит `scaleVersion` из Задачи 1 (для записей без него —
      трактовка «целые единицы»)
- [ ] ⚠️ **и снимок валюты комнаты на момент ввода.** Версии шкалы мало:
      расход введён в пустой долларовой комнате, лежит в очереди, а с другого
      устройства комнату — пока пустую ДЛЯ СЕРВЕРА — перевели на иены (Задача 3
      это разрешает). Сервер увидит иены и прежнюю версию шкалы, но не узнает,
      что 2080 вводили как 20.80 $
- [ ] при несовпадении валюты — понятный отказ и вопрос человеку, а НЕ пересчёт
      между валютами: курса у нас нет и быть не должно
- [ ] тест ровно на этот случай: очередь пережила смену валюты комнаты
- [ ] ⚠️ **в записи не одна сумма**: `recipientSums`, позиции чека с ценами и
      `shares[].amount` (`ios/Splitty/Core/OutboxStore.swift:9`,
      `android/app/src/main/java/com/zagir/splitty/data/OutboxStore.kt:66`).
      Домножать надо каждое денежное поле, забытое — расход в сто раз меньше
- [ ] запись без отметки трактуется как целые единицы и домножается при
      отправке
- [ ] тест на обоих клиентах: очередь, записанная прежней версией, уходит
      верной суммой

### Task 11 (фаза 4): Read-кеш на диске переживает обновление
**Files:**
- Modify: `ios/Splitty/Core/OfflineStore.swift`, `ios/Splitty/Core/DataRepo.swift`
- Modify: `android/app/src/main/java/com/zagir/splitty/data/ApiCache.kt`

⚠️ **Блокер, найден на ревью.** Оба клиента держат на диске не только очередь
отправки, но и КЕШ ЧТЕНИЯ: комнаты, операции, долги, друзья, статистика.
Версии формата у него нет — `OfflineStore.read` просто декодирует JSON в
текущую модель. После обновления файл, записанный прежней сборкой, прочитается
новой: `sum: 1200` уедет в поле минорных единиц и покажется как 12.00 вместо
1 200.

Хуже, что починить это пересчётом на месте нельзя: у операции нет валюты, она
известна только родительской комнате (`ios/Splitty/Core/Models.swift:315`
против `:555`), и в кеше операций контекста нет.

- [ ] версия формата у кеша; при несовпадении денежные ключи выбрасываются, а
      не пересчитываются
- [ ] ⚠️ **справочник валют лежит в том же кеше** (`DataRepo.swift` ключ
      `currencies`, `ApiCache.Keys.CURRENCIES`) и читается «сначала из кеша».
      Его тоже инвалидировать, иначе шкала останется прежней
- [ ] ⚠️ **первый запуск без сети и без справочника**: шкала неизвестна.
      Показывать суммы как есть и не давать вводить дробь, пока справочник не
      приехал — молчаливый ноль покажет 2080 вместо 20.80
- [ ] пустой кеш — штатное состояние (первый запуск), проверить, что экран не
      ломается, а идёт в сеть
- [ ] тест на обоих клиентах: файл кеша от прежней сборки не даёт новой
      показать неверную сумму

### Task 12 (фаза 4): Ввод и вывод на iOS
**Files:**
- Modify: `ios/Splitty/Core/Money.swift`, `ios/Splitty/Features/Expense/*`

- [ ] `money()` печатает дробную часть по экспоненте валюты
- [ ] ⚠️ **клавиатура сейчас без разделителя**: `.numberPad` в пяти местах на
      iOS и `KeyboardType.Number` в пяти на Android. Дробь ввести физически
      нечем. У валют со шкалой больше нуля — `.decimalPad`/`Decimal`, у иены
      прежняя
- [ ] ⚠️ **разбор суммы есть и вне расхода**: погашение долга
      (`ios/Splitty/Features/SettleUp/SettleUpView.swift:296`, `:312`, `:345`,
      `android/app/src/main/java/com/zagir/splitty/ui/settleup/SettleUpScreen.kt:478`) и позиции чека
      (`AddExpenseView.swift:2577`)
- [ ] поведение поля ВО ВРЕМЯ набора: «20.» и «0,5» — промежуточные состояния,
      а не ошибка; ошибку показывать по завершении ввода
- [ ] ⚠️ **разделитель один, группировка запрещена**: принимать и точку, и
      запятую можно только если в строке ровно один разделитель. «1,234» из
      буфера обмена иначе это и «1.234», и «1234» — угадывать нельзя, надо
      отказывать
- [ ] больше знаков, чем у валюты, — ошибка ввода с объяснением
- [ ] тесты: ввод «20,8» и «20.8» дают одно и то же; «20.888» отвергается;
      у иены дробная часть не вводится вовсе
- [ ] прогнать набор iOS

### Task 13 (фаза 4): Ввод и вывод на Android
**Files:**
- Modify: `android/app/src/main/java/com/zagir/splitty/core/money/Money.kt`, `android/app/src/main/java/com/zagir/splitty/ui/expense/*`

- [ ] те же правила и те же тексты ошибок, что на iOS
- [ ] ⚠️ **новые строки — на десять языков**: тексты ошибок ввода живут в
      `ios/Splitty/Localizable.xcstrings` и
      `android/app/src/main/res/values-*`, языков десять. Каталожные тесты
      уронят сборку на непереведённом ключе — это и есть срок
- [ ] тест на разделитель в локалях, где он запятая (de, fr, it, pt-BR)
- [ ] прогнать набор Android

### Task 14 (фаза 5): Распознавание, бот и напоминания
**Files:**
- Modify: `internal/ai/prompt.go`, `internal/rest/parse_handler.go`
- Modify: `internal/bot/operation_screen.go`

- [ ] ⚠️ **разбирать ответ модели точно, а не через `float64`**: после
      разбора в float «20.80» и «20.8» неразличимы, и отвергнуть лишний знак
      уже нечем. Брать `json.Number`/сырую строку и переводить в минорные
      разбором десятичной записи — либо просить у модели сразу целое минорное
- [ ] в промпте сказать прямо: цены как в жизни, «20.8» допустимо; передавать
      модели число допустимых знаков и правила разделителя — иначе
      индонезийские «20.800» будут поняты как 20.8
- [ ] ⚠️ **денежные лимиты заданы в нынешних единицах**: 100 000 000 на цену и
      1 000 000 000 на итог (`parse_convert.go:21`, `parse_sanitize.go:60`).
      Механическая смена единицы урежет потолок в сто раз; умножение констант
      требует заново доказать, что произведение `amount*weight` не переполнится
- [ ] `/parse` отдаёт минорные поля ДОПОЛНИТЕЛЬНО, сохраняя округлённые
      старые: клиент, уже умеющий копейки, иначе применит старый черновик в
      прежней единице
- [ ] бот разбирает дробный ввод вместо `strconv.Atoi`
- [ ] бот пишет операцию в минорных единицах: он создаёт расходы напрямую,
      мимо REST, и забытое поле здесь означает расход в сто раз меньше
- [ ] ⚠️ **бот считает деньги во float во многих местах**, не только парсит
      ввод: деление долей, вычитание, округление и приведения к `int`
      (`operation_screen.go:350`, `:518`, `:635`, `:703`, `:1028`, `:1140`,
      `:1245`). Перевести все, включая черновики и ручное распределение
- [ ] ⚠️ **отдельный ботский путь погашения долга** (`:2062`, `:2157`, `:2234`)
      сравнивает с `Debt.Sum` и сохраняет операцию напрямую
- [ ] ⚠️ **сумма долга лежит СТРОКОЙ в кнопке** (`:2067`,
      `ExternalId: strconv.Itoa(debt.Sum)`). Кнопки живут в истории Telegram
      вечно: нажатая завтра старая кнопка передаст сумму в прежних единицах.
      Нужна отметка единицы в callback-данных и трактовка старых как целых
- [ ] форматирование не только в `MoneyWithSymbol`: есть `moneySpace`
      (`tg_helper.go:381`), голый `%d` в позициях (`operation_items.go:87`),
      статистика бота (`statistic_screen.go:56`), нотифаер
      (`internal/bot/notifier.go:181`) и напоминания (`reminders/text.go:49`)
- [ ] ⚠️ **порог «шума» в напоминаниях** (`reminders/reminders.go:100`) задан
      в единицах валюты: после перехода он превратится из «до N рублей» в «до
      N копеек» и перестанет отсекать пыль
- [ ] тексты бота и пушей печатают дробь через общий `MoneyWithSymbol`
- [ ] тесты разбора: «двадцать восемьдесят», «20,8», «20.80»

### Task 15 (вне фаз): Переименование тусы

**Files:**
- Modify: `internal/rest/server.go` (роут), `internal/rest/handlers.go`
- Modify: `internal/repository/repository.go` (обновление имени)
- Modify: `ios/Splitty/Features/Groups/GroupSettingsView.swift`, `ios/Splitty/Core/APIClient.swift`
- Modify: `android/.../ui/groups/GroupDetailScreen.kt`, `GroupDetailViewModel.kt`,
  `data/SplittyRepository.kt`, `core/network/SplittyApi.kt`
- Modify: `internal/bot/setting_screen.go`

⚠️ **Название тусы сегодня нельзя изменить нигде.** Оно задаётся один раз при
создании (`handlers.go:373`, `internal/bot/room_creating.go:96`), а среди роутов
есть `PUT .../avatar` и `PUT .../currency` — переименования нет. В настройках
тусы на iOS секции с названием тоже нет (`GroupSettingsView.swift:80`), у бота
экран настроек предлагает только валюту, архив и выход. Опечатка в названии —
навсегда.

- [ ] `PUT /api/v1/rooms/{roomId}/name`, доступ проверяется `roomForMember` —
      как у смены валюты (`handlers.go:506`). Владельца тусы в модели нет
      (`api.Room` знает только `Members`), поэтому «только создатель» пришлось
      бы изобретать вместе с полем — не делаем
- [ ] валидация та же, что при создании: обрезка пробелов, непустое,
      не длиннее `maxRoomNameLen`. ⚠️ Вынести в общую функцию: два места с
      одним правилом разъезжаются молча
- [ ] секция «Название» в настройках тусы на iOS и Android
- [ ] бот: экран переименования рядом со сменой валюты, ввод текста тем же
      состоянием, что и `RoomSetName` (`room_creating.go:64`)
- [ ] ⚠️ **имя уходит наружу**: в пуши, в заголовки, в кнопку приглашения
      (`btn_share_room` подставляет `room.Name`). Уже отправленный пуш
      переименование не догонит — это ожидаемо, но проверить, что нигде имя не
      закешировано на клиенте дольше `dataVersion`
- [ ] строки на десяти языках
- [ ] тесты: переименовал участник — можно; чужой — 403/404 как у валюты;
      пустое и слишком длинное — 400; пробелы по краям срезаются

### Task 16 (вне фаз): Валюта при создании тусы

**Files:**
- Modify: `internal/rest/handlers.go` (`createRoomRequest:359`, `handleCreateRoom:364`)
- Modify: `ios/Splitty/Features/Groups/CreateGroupView.swift`
- Modify: `android/.../ui/groups/GroupsListScreen.kt`, `GroupsListViewModel.kt`

⚠️ **Валюту при создании не спрашивают вовсе.** Комната создаётся без неё, а
`roomCurrencyCode` (`dto.go:458`) подставляет `DefaultCurrency = "RUB"`. Человек
узнаёт об этом позже, когда менять уже поздно: смена валюты суммы не
пересчитывает, и об этом честно написано в самом окне смены. Дорогое
спрашиваем заранее — дешёвое оставляем в настройках.

- [ ] `createRoomRequest` получает необязательное `currency`; пустое — прежнее
      умолчание, чтобы установленные сборки продолжали создавать тусы как
      раньше
- [ ] код проверяется тем же `api.IsSupportedCurrency`, что и смена валюты
- [ ] шкала новой тусы берётся из умолчания валюты (Задача 1) в том же
      запросе — а не «создали и сразу переключили»
- [ ] на экране создания валюта — не вопрос, а подтверждение: подставляем из
      последней созданной тусы человека, иначе из региона телефона, иначе
      `DefaultCurrency`
- [ ] ⚠️ регион телефона может дать валюту, которой нет в справочнике, — тогда
      молча падаем на умолчание, а не показываем пустую строку
- [ ] **копейки на этот экран не выносим.** Включение шкалы обратимо и точно
      (Решение 1), а валюта — нет. И вопрос «считать ли копейки?» задаётся до
      первого расхода, когда взвесить его человеку ещё не на чем
- [ ] **один экран, а не мастер.** Создание — воронка, туса заводится один раз
      на поездку, и каждый лишний шаг тут только теряет людей
- [ ] **бот оставляем как есть**: создаёт с умолчанием, валюта меняется в его
      настройках, где она уже есть. Второй шаг в диалоге бота стоит дороже, чем
      второе поле в форме приложения
- [ ] тесты: без валюты — рубль; с валютой — она; неизвестный код — 400

### Task 17: Verify acceptance criteria
- [ ] `GOTOOLCHAIN=go1.22.12 go build ./... && go vet ./...`, тесты на 1.24.6
- [ ] `cd ios && xcodegen generate && xcodebuild test -project Splitty.xcodeproj -scheme Splitty -destination "platform=iOS Simulator,name=iPhone 17 Pro,OS=26.2" -only-testing:SplittyTests`
- [ ] `cd android && ./gradlew :app:testDebugUnitTest :app:lintVitalRelease`
- [ ] расход 20.8 $ на троих: доли 6.94 / 6.93 / 6.93, сумма сходится
- [ ] старая сборка клиента показывает ту же комнату без копеек и не ломается
- [ ] расхождений долгов до и после перехода нет (сверка из Задачи 8)
- [ ] расход, положенный в очередь прежней версией, уходит верной суммой
- [ ] бот и пуш про тот же расход печатают 20.80 $, а не 20 $
- [ ] тусу переименовали в приложении — новое имя видно в боте и в пушах
- [ ] туса, созданная с евро, сразу и в евро, и со шкалой евро — без второго
      запроса на смену валюты

### Task 18: [Final] Обновить документацию
- [ ] `docs/API.md` — единица денег, экспонента, срок жизни старого поля
- [ ] `CLAUDE.md` — правило «в модели только минорные единицы»
- [ ] перенести план в `docs/plans/completed/`

## Порядок выкатки

«Арифметика, миграция и API одним куском» — шаг, который нельзя откатить:
между ними база и код расходятся в единице. Поэтому пять фаз, и дробный ввод
включается последним.

**Фаза 1. Совместимый бэкенд, фича выключена.** Сервер умеет читать документы
и со старым полем, и с новым (различая «поля нет» от нуля), пишет оба, отдаёт
оба. Внутренняя арифметика ещё старая. Откат на прежнюю версию безопасен:
новые поля она просто не читает.

**Фаза 2. Миграция.** Под уже совместимыми писателями — иначе откатившийся
или недоехавший экземпляр затрёт минорные поля при первой же правке. После
миграции сверить: НОВЫХ расхождений «сумма долей ≠ сумма» не появилось, а
каждое прежнее получило исход — вылечено, оставлено как есть или отложено
разбираться. Требовать совпадения множеств нельзя в обе стороны: полного
равенства не было и до миграции (`docs/API.md`, `debtsUnavailable`), а
пересчёт равного деления через `ShareOf` часть старых расхождений законно
уберёт — 100 на троих было 33.33 у каждого, станет 3334+3333+3333.
Сверить заодно список «неисчислимых» комнат и долги по каждой.

**Фаза 3. Внутренняя арифметика.** Долги, статистика, напоминания, бот,
админка и выгрузка наружу (Задача 9) переходят на минорные как на
единственный источник. Здесь
же сверка долгов до и после.

**Фаза 4. Клиенты.** Одной сборкой: миграция очереди, версия read-кеша,
справочник шкал доступен до денежных экранов. Дробный ввод ещё выключен.

**Фаза 5. Включение.** Дробный ввод, дробный ответ AI, дробные тексты бота.

⚠️ **Чем именно включается — решить до начала.** Фича-флага в продукте нет
(ни серверного, ни клиентского). Три варианта, и выбрать надо один:

Отвергнутые варианты: второй релиз клиента (просто, но включение и откат идут
через ревью магазинов, то есть дни) и смена шкалы (см. ниже — так делать
нельзя).

**Выбрано: признак в ответе сервера** (`fractionalInput` рядом со
справочником) — один релиз клиента, включение без магазина. ⚠️ Это ДВА разных понятия, и
  путать их нельзя: умение читать новый контракт (клиент всегда предпочитает
  присутствующее минорное поле, флаг тут ни при чём) и разрешение ПИСАТЬ
  дроби. Выключенный флаг обязан отвергать дробные значения на всех входах —
  REST расхода, погашения и позиций, разбор AI, бот, — а не прятать клавишу в
  приложении. Уже сохранённые дроби при этом продолжают читаться точно;
- **сменой шкалы с нуля на два** — заманчиво и НЕВЕРНО: шкала тусы (Решение 1) —
  это пересчёт всех её сумм, ради которого и заведены `scaleVersion` с миграцией.
  Она отвечает на вопрос «в этой тусе считают копейки?», а не на «умеет ли
  система их принимать», и выключателем фичи служить не может.

Признак даёт откат без магазина, если дроби где-то поедут.

**Клиент новее сервера.** Если сервер откатили, минорных полей в ответе нет —
и не у одной суммы, а по всему дереву: доли, цены позиций, `shares[].amount`,
долги и сводные числа. Ни одно из них не может быть обязательным при разборе.

Шкалу в этом случае брать неоткуда: старый `/currencies` отдаёт строки без
`displayExponent` и `scaleVersion`. Единственное честное поведение — считать
шкалу нулевой и показывать числа как целые, а не выводить её из текущего
справочника. После включения дробного ввода такой откат уже не обещает
точности, поэтому он и запрещён следующим абзацем.

**Граница отката.** Вернуться на бэкенд без минорных единиц безопасно ровно
до фазы 5: пока дробных записей нет, старый код теряет только служебные поля,
и восстановительный проход из Задачи 6 их достроит. После включения дробного
ввода откат уничтожает настоящие копейки — там уже только откат на совместимую
сборку или починка вперёд.

Прод разворачивается одним контейнером с проверкой здоровья и откатом
(`deploy/deploy.sh`), не несколькими репликами, — поэтому «дренаж старых
экземпляров» здесь не нужен. Но фаза 1 всё равно обязана быть отдельной: она
защищает не от параллельных реплик, а от ОТКАТА.

## Три релиза, а не один

План крупный, и целиком он не нужен, чтобы начать приносить пользу. Внутри —
три куска, каждый со своим смыслом:

**A. Бэкенд, совместимость и миграция** (Задачи 1–9). Человек не видит
ничего. Выигрыш всё равно есть: float уходит из долгового движка, а выгрузка
расходов наружу перестаёт отдавать числа, которые получатель не может
истолковать. Задачи 4 и 9 полезны и без всяких дробей.

**B. Гигиена клиентов** (Задачи 10–11). Версия у кеша и очереди на диске.
Тоже незаметно снаружи, и защищает от любого будущего изменения модели, не
только от этого.

**C. Включение** (Задачи 12–14, 17–18). Единственный кусок, где нужны решения
из раздела выше.

**D. Настройки тусы** (Задачи 15–16). Переименование и выбор валюты при
создании. К дробям отношения не имеют и ни от чего не зависят — попали сюда
потому, что трогают те же два экрана: создание тусы и её настройки. Делать
вместе дешевле, чем возвращаться в них дважды.

Самое маленькое, что имеет смысл выпустить отдельно и хоть завтра: Задача 9
(конверт выгрузки с валютой) и Задача 4 (запрет смены валюты между шкалами) —
они ни от чего не зависят. Туда же — Задача 15: переименования тусы нет
сегодня вообще, и дробей оно не ждёт.

## Post-Completion

*Только человек:*

- ⚠️ **снять дамп базы перед миграцией.** `deploy/deploy.sh` откатывает образ,
  но не данные, а миграция трогает все документы комнат — операции лежат
  внутри `room`;
- предупредить получателя выгрузки расходов о новом формате (Задача 9): это
  чужая система, и её никто из нас не чинит;

- выкатить бэкенд ДО публикации новых сборок: старый сервер не знает
  `sumMinor` и потеряет копейки, присланные новым клиентом;
- проверить на живых данных, что суммы в старых комнатах не изменились;
- решить, когда убирать старое поле `sum`. ⚠️ Измерить эту долю сегодня нечем:
  сервер видит только наличие заголовка версии, но не считает версии
  (`quota.go:36`). Либо завести счётчик версий клиентов, либо признать, что
  срок назначается на глаз.

*Отдельными задачами:*

- `docs/API.md:12` перечисляет справочник валют без иены, юаня, воны и реала —
  список устарел сегодня же, вместе с этим планом его и поправить;
- ⚠️ **валюты с экспонентой 3** (динар, динары Туниса и Иордании) в
  справочнике не заводились и этим планом не покрыты;
- отображение дробей в Telegram-боте на языках, где разделитель — запятая:
  бот знает только `ru`/`en`, и общий форматтер там ведёт себя иначе, чем в
  приложении.
