---
worth: later
added: 2026-08-28
---
# обновить agterm до 0.25.0

Стоит 0.24.0, вышла 0.25.0. Ничего не блокирует, поэтому отложено.

Что чинит:
- `ssh` внутри agterm умирал сразу при включённой ghostty-интеграции `ssh-env` / `ssh-terminfo`
- agent-status хуки молча переставали работать, если бандл приложения переезжал после установки
- NUL в `session.type` обрезал ввод, но команда отвечала ok — Return при этом всё равно уходил
- команда из правой панели сплита экспортировала `$AGT_SESSION_PWD` от левой
- backlog-picker больше не предлагает удалять пункты `worth: no`, чья работа как раз в том, чтобы не находить проблему заново

Обновление: `brew upgrade --cask agterm`, затем ⌘Q и перезапуск.
