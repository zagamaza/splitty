<?xml version="1.0" encoding="utf-8"?>
<!-- RELEASE-вариант: cleartext ЗАПРЕЩЁН глобально и без исключений — бэкенд живёт
     на https://splitor.zagirnur.dev. Не добавлять сюда разрешающих domain-config:
     каждый запрос несёт Authorization: Bearer с 90-дневным JWT, по plaintext это
     перехват сессии. Инвариант стережёт SecurityConfigTest, парная константа —
     SessionStore.DEFAULT_BASE_URL. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
