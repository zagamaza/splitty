package com.zagir.splitty

/**
 * Сколько ждать асинхронного результата в тестах, прежде чем считать его
 * зависшим.
 *
 * Это предохранитель от вечного ожидания, а не проверка скорости: наборы ходят
 * в настоящий DataStore и настоящий MockWebServer на настоящих диспетчерах, и
 * сколько займёт файловая операция, решает загрузка машины, а не код. Прежние
 * пять секунд выглядели с запасом ровно до первой параллельной задачи: под
 * занятым CPU `AppRootJoinTest` падал примерно в трети прогонов, всегда
 * таймаутом на потоке DataStore и всегда в разных тестах — то есть красным без
 * повода. На поиск настоящего зависания разницы нет: и то и другое ждёт
 * заведомо дольше любой здоровой операции.
 */
const val IO_WAIT_MS = 30_000L
